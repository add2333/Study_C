#include <stdio.h>
int main(void)
{
    int num=1;
    for (printf("Keep entering the number!"); num!=233;)
        scanf("%d", &num);
    printf("That's the number I need!");

    getchar();
    getchar();
    getchar();

    return 0;
}